App for searching tweets based on user input.

Steps to run:
* Clone the repo
* Navigate to the cloned folder

      npm install 
      npm start
* Navigate to the server folder inside the cloned folder
      
      npm install 
      npm start
* Navigate to localhost:3000 to use the app
